#include "settlement.h"
#include <vector>

void generate_land(int point[2], int size){
    vector<vector<int>> land_list;
    land_list[0][0] = point[0];
    land_list[0][1] = point[1];
    vector<int[2]> deleted_land;
    for(int count = 0;count < size; ++count){
        //randomный выбор из ланд листа
        //mm[choice0, choice1] = 1
        //if(mm[ch0 + 1, ch1]==0 and (ch0+1,ch1) not in land_list)
    }
}
